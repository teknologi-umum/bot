{{#expect title, body, url }}

<b>{{ (title == undefined ) ? title : (title.length > 50) ? title.substr(0, 49) + '...' : title }}</b>\n

{{ (body == undefined ) ? body : (body.length > 300) ? body.substr(0, 299) + '...' : body }}

Baca lanjutannya: {{ url }}